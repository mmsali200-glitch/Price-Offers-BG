{
  "name": "price-offers-bg",
  "version": "1.0.0",
  "private": true,
  "description": "Price quote extractor web app - static site",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
